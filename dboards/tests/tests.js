/* globals $ */
/* eslint-env node, dirigible */
(function(){
"use strict";

var boardDAO = require("discussion_boards/lib/board_tags_dao").get();
/*var boardDAO = require("discussion_boards/lib/board_dao").get();
console.info(boardDAO.isLocked(46));
console.info('has list: '+ boardDAO.list!==undefined);

var boardStatsDAO = require("discussion_boards/lib/board_stats_dao").get();
console.info(boardStatsDAO.isLocked(46));
console.info('has list: '+ boardStatsDAO.list!==undefined);
*/})();
