{
  "viewName":"DIS_DISCUSSION_STATS",
  "query":"SELECT
	MAX(DISC_LASTMODIFIED_TIME) AS LATEST_UPDATE_TIME,
	COUNT(DISC_REPLY_TO_DISC_ID) AS REPLIES,
	COUNT(DISTINCT DISC_USER) AS PARTICIPANTS,
	DISB_ID	
FROM DIS_COMMENT, DIS_BOARD
WHERE DISC_DISB_ID = DISB_ID
GROUP BY DISB_ID
ORDER BY LATEST_UPDATE_TIME, REPLIES, PARTICIPANTS DESC",
    "dependencies": [
        {"name": "DIS_COMMENT", "type": "TABLE"},
        {"name": "DIS_BOARD", "type": "TABLE"}
        ]
}
