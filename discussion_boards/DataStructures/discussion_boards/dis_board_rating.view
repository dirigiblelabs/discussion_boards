{
  "viewName":"DIS_BOARD_RATING",
  "query":"SELECT
	DISB_ID,
	COUNT(DISTINCT DISV_ID) AS TOTAL_VOTES, 
	SUM(DISV_VOTE) AS RATING
FROM DIS_BOARD, DIS_BOARD_VOTE
WHERE DISB_ID = DISV_DISB_ID
GROUP BY DISB_ID, 
	     DISB_PUBLISH_DATE
ORDER BY RATING, TOTAL_VOTES, DISB_PUBLISH_DATE DESC"
}
