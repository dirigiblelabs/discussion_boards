{
  "viewName":"DIS_BOARD_RATING",
  "query":"SELECT
	DISB_ID,
	COUNT(DISTINCT IDFV_ID) AS TOTAL_VOTES, 
	SUM(IDFV_VOTE) AS RATING
FROM DIS_BOARD, IDF_IDEA_VOTE
WHERE DISB_ID = IDFV_IDFI_ID
GROUP BY DISB_ID, 
	     DISB_PUBLISH_DATE
ORDER BY RATING, TOTAL_VOTES, DISB_PUBLISH_DATE DESC"
}